# Keep correctness, performance, deprecation, and dead-code checks blocking.
# ST1005 remains excluded because several errors are also user-facing diagnostic
# sentences whose established casing is part of the UI and exported logs.
checks = ["inherit", "-ST1005"]
